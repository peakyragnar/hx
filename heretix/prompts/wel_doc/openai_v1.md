Provider notes (OpenAI GPT-5):
- Use JSON schema mode; return only the JSON object.
- If the platform injects reasoning metadata, keep it under 200 tokens and ensure the final JSON object is the last content block.
- Keep bullet strings under 220 characters.
