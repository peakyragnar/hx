"""
Test suite for Heretix Raw Prior Lens (RPL) statistical components.

Focuses on pure statistical correctness without external dependencies.
Tests are split into fast (pre-commit) and slow (CI/pre-push) categories.
"""